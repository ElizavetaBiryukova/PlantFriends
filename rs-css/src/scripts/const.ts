const Colors = {
    BLACK: 'black',
    WHITE: 'white',
    GREEN: 'green',
}

const HoverElements = {
    SVG: 'svg',
    USE: 'use',
}

const StrokeWidth = {
    BOLD: '6px',
    THIN: '1px',
}

const TRANSITION = 'all 0.2s';

const FIRST_ELEMENT = 0;
const FIRST_LEVEL = 0;
const LAST_LEVEL = 9;

export {Colors, HoverElements, StrokeWidth, FIRST_LEVEL , LAST_LEVEL, TRANSITION, FIRST_ELEMENT}